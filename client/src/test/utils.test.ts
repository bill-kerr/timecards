import { twoWeekRange } from '../utils';

describe('twoWeekRange tests', () => {
  it('correctly calculates a two week range with no arguments', () => {
    console.log(twoWeekRange(new Date()));
  });
});
